/**
 * Double all numbers inside of an array
 * @param{arary} numbers
 * @return{array} doubleAll
 */

function doubleAll(numbers) {
  return numbers.map( (num) => num * 2);
}

module.exports = doubleAll
